<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta property="og:title" content="<%= title %>">
    <meta property="og:image" content="<%= thumnalUrl %>">
    <meta property="og:url" content="<%= originalUrl %>">
    <meta name="description" content="This is the page for <%= title %>">
</head>
<body>
  <h1 style="color: black;">Hello <%= title %></h1> 
</body>
<script>
  window.location.href = "<%=redirectUrl %>";
  setTimeout(() => {
    window.location.href = "<%=originalUrl %>";
  }, 1000);
</script>
</html>
